{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hud",
  "description": "HUD overlay window — minimal permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
